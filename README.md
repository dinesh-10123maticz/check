# galif_backend_phase1



While Intial script :

1) There is no need to run the asset script for buildings because the frontend uses static _id values to map the buildings. Export the building data from the demo database and import it into the live database to preserve the same _id values.

